export class DropBuilder {
    airdropToken;
    finalAirdropRecipients;//{0xnftContract:{"1":"1000000"}} use BigNumber.toString()
    finalAirdropRecipientsCsv; // for idsAndAmounts in finalAirdropRecipients; balances.push([nftContract, ...Object.entries(idsAndAmounts)]) 
                            
}